export const deleteModalTitle = "Delete this task?";

export const getDeleteModalText = (taskName: string) => {
    return `Are you sure you want to delete the "${taskName}" task and its subtasks? This action cannot be reversed.`;
};

export const COLUMN_MAIN_CONTAINER_GAP = 10;
